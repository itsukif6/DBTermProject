CREATE TABLE SalesData (
    ID INT,
    Product VARCHAR(255),
    Area VARCHAR(255),
    Season VARCHAR(255),
    ProductCategories VARCHAR(255),
    Period VARCHAR(255),
    PRIMARY KEY(ID)
);
